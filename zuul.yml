server:
  port: 8081
  
spring:
  application:
    name: zuul
    
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
  
zuul:
  routes:
    pdf:
      path: /pdf/**
      serviceId: pdf
  host: 
    socket-timeout-millis=30000
